services:
  personal-service:
    build:
      context: .
      # Assuming a standard Dockerfile exists or will be created here
      dockerfile: Dockerfile
    container_name: suances-personal-service
    ports:
      - "8085:8085"
    env_file:
      - ../../.env
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - DB_HOST=postgres
      - REDIS_HOST=redis
    networks:
      - suances-network

networks:
  suances-network:
    external: true
    # We reference the network created by docker-compose-infra.yml
    name: suances_suances-network
